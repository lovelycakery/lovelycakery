# 版本管理系統使用說明

## 📋 系統簡介

這個專案使用 `versions.json` 文件記錄版本資訊（版本號、說明、commit/tag 等），用來快速回溯「這次改了什麼」。  
注意：repo 內 **沒有**自動產生版本/自動回復的腳本；`versions.json` 與 git tag 需由你（或協作者/AI）在變更時更新。

## 🚀 如何使用

### 1. 創建新版本

**對話範例：**
```
「創建版本 v1.1，說明是：新增日曆功能」
「建立版本 v2.0，說明：重大更新，重新設計首頁」
```

**建議流程：**
- 確認變更已 commit（有對應 commit hash）
- （可選）建立 git tag（如 `v1.1.4`）
- 更新 `versions.json` 記錄版本資訊（建議把 `files_changed` 列出來）

### 2. 列出所有版本

**對話範例：**
```
「列出所有版本」
「顯示版本列表」
「有哪些版本？」
「查看所有版本」
```

**我會顯示：**
- 所有版本的編號和說明
- 創建日期
- Git commit 信息

### 3. 恢復到指定版本

**對話範例：**
```
「恢復到版本 v1.0」
「回到版本 v1.1」
「切換到版本 v2.0」
```

**建議流程：**
- 從 `versions.json` 找到對應的 `git_commit` 或 `git_tag`
- 用 git 恢復（例如 `git checkout <tag_or_commit> -- .`）
- 視需要更新 `current_version`（或建立新版本記錄）

### 4. 查看特定版本信息

**對話範例：**
```
「版本 v1.0 的說明是什麼？」
「v1.1 是什麼時候創建的？」
```

## 📝 版本記錄格式

版本信息記錄在 `versions.json` 文件中，格式如下：

```json
{
  "versions": [
    {
      "version": "v1.0",
      "description": "初始版本",
      "git_commit": "完整的 commit hash",
      "git_commit_short": "短 commit hash",
      "git_tag": "v1.0",
      "created_at": "2025-12-25",
      "created_by": "創建者名稱",
      "files_changed": []
    }
  ],
  "current_version": "v1.0"
}
```

## 🎯 完整使用範例

### 範例 1：創建多個版本

```
您：「創建版本 v1.0，說明是：初始版本」
我：✅ 已創建版本 v1.0
   說明：初始版本
   Git commit: d9cabc2

您：「創建版本 v1.1，說明是：新增日曆功能」
我：✅ 已創建版本 v1.1
   說明：新增日曆功能
   Git commit: xxxxxxx

您：「創建版本 v1.2，說明是：優化首頁樣式」
我：✅ 已創建版本 v1.2
   說明：優化首頁樣式
   Git commit: yyyyyyy
```

### 範例 2：查看和恢復版本

```
您：「列出所有版本」
我：📋 版本列表：
   v1.0 - 初始版本 (2025-12-25)
   v1.1 - 新增日曆功能 (2025-12-26)
   v1.2 - 優化首頁樣式 (2025-12-27)

您：「恢復到版本 v1.0」
我：🔄 正在恢復到版本 v1.0...
   ✅ 恢復完成！已將所有文件恢復到 v1.0 版本
```

## ⚠️ 注意事項

1. **版本編號格式**：建議使用 `v1.0`、`v1.1`、`v2.0` 等格式
2. **恢復前確認**：恢復操作會覆蓋當前文件，請確認後再執行
3. **Git 提交**：建議先 commit，再 tag，再更新 `versions.json`
4. **版本唯一性**：每個版本編號只能使用一次

## 🔧 技術細節

- **版本記錄**：`versions.json` 文件
- **Git 標籤**：每個版本會創建對應的 git tag
- **恢復方式**：使用 `git checkout <commit/tag> -- .` 恢復文件

## 📂 文件位置

- 版本記錄文件：`versions.json`
- 使用說明：`版本管理使用說明.md`（本文件）

---

**提示**：如果你是用 AI/協作者一起維護，可以用「對話」把版本資訊整理出來，再由其中一方更新 `versions.json` 與（可選）git tag。  
（如果你想把這件事「半自動化」：可以後續加一個 `scripts/bump_version.py` 或 shell 腳本來產生 tag/更新 `versions.json`，但目前專案刻意保持零 build、零工具依賴。）

